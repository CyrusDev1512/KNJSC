---
name: test-driven-development
description: "Use before implementing new behavior or fixing a behavioral bug that needs a regression test. Excludes ordinary questions, source exploration, and documentation-only edits."
---

Cầu nối cho Claude Code, sinh bởi `scripts/dong-bo-skill.py --tao-cau-noi`. Đừng sửa
tay: sửa xong ở nguồn thì chạy lại lệnh đó.

Nội dung thật của skill này nằm ở `.agents/skills/test-driven-development/SKILL.md`. Đọc tệp đó và làm đúng theo nó.
Mọi đường dẫn tương đối trong đó (`reference/`, `references/`, `scripts/`, `agents/`)
tính từ thư mục `.agents/skills/test-driven-development/`. Quy tắc dự án ở `AGENTS.md` đứng trên skill.
