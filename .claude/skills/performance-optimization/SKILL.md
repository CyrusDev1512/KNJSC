---
name: performance-optimization
description: "Design or change code for a concrete performance requirement or measured bottleneck in Django, PostgreSQL, or browser data handling. Use for optimization decisions and implementation, not routine edits or load-test execution alone."
---

Cầu nối cho Claude Code, sinh bởi `scripts/dong-bo-skill.py --tao-cau-noi`. Đừng sửa
tay: sửa xong ở nguồn thì chạy lại lệnh đó.

Nội dung thật của skill này nằm ở `.agents/skills/performance-optimization/SKILL.md`. Đọc tệp đó và làm đúng theo nó.
Mọi đường dẫn tương đối trong đó (`reference/`, `references/`, `scripts/`, `agents/`)
tính từ thư mục `.agents/skills/performance-optimization/`. Quy tắc dự án ở `AGENTS.md` đứng trên skill.
