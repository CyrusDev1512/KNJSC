---
name: web-perf
description: "Measure and diagnose browser page loading, scrolling, selection, typing lag, rendering, and memory. Use for frontend performance evidence; excludes SQL-only optimization and API load testing."
---

Cầu nối cho Claude Code, sinh bởi `scripts/dong-bo-skill.py --tao-cau-noi`. Đừng sửa
tay: sửa xong ở nguồn thì chạy lại lệnh đó.

Nội dung thật của skill này nằm ở `.agents/skills/web-perf/SKILL.md`. Đọc tệp đó và làm đúng theo nó.
Mọi đường dẫn tương đối trong đó (`reference/`, `references/`, `scripts/`, `agents/`)
tính từ thư mục `.agents/skills/web-perf/`. Quy tắc dự án ở `AGENTS.md` đứng trên skill.
